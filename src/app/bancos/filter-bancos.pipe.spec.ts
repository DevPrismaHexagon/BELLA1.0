import { FilterBancosPipe } from './filter-bancos.pipe';

describe('FilterBancosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBancosPipe();
    expect(pipe).toBeTruthy();
  });
});
