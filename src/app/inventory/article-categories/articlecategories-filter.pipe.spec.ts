import { ArticlecategoriesFilterPipe } from './articlecategories-filter.pipe';

describe('ArticlecategoriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticlecategoriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
