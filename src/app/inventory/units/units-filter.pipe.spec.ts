import { UnitsFilterPipe } from './units-filter.pipe';

describe('UnitsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
