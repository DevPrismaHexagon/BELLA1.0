import { LocationVentasPipe } from './location-ventas.pipe';

describe('LocationVentasPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationVentasPipe();
    expect(pipe).toBeTruthy();
  });
});
